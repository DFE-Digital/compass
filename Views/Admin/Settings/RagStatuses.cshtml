@model List<Compass.Models.RagStatusLookup>
@{
    ViewData["Title"] = "RAG Statuses";
    var currentAction = ViewContext.RouteData.Values["Action"]?.ToString() ?? "";
    var currentController = ViewContext.RouteData.Values["Controller"]?.ToString() ?? "";
}

@section Breadcrumbs {
    <nav aria-label="Breadcrumb">
        <ol class="breadcrumb mb-0 pl-4 breadcrumb-standard">
            <li class="breadcrumb-item">
                <a href="@Url.Action("Index", "Home")" class="breadcrumb-link-standard">Home</a>
            </li>
            <li class="breadcrumb-item">
                <a href="@Url.Action("Settings", "Admin")" class="breadcrumb-link-standard">Settings</a>
            </li>
            <li class="breadcrumb-item active breadcrumb-active-standard" aria-current="page">RAG Statuses</li>
        </ol>
    </nav>
}

<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0">RAG Statuses</h1>
            </div>
            <div class="col-sm-6">
                <div class="float-right">
                    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#createRagStatusModal">
                        Create new
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<section class="content">
    <div class="container-fluid">
        @if (TempData["SuccessMessage"] != null)
        {
            <div class="alert alert-success alert-dismissible fade show">
                <button type="button" class="close" data-dismiss="alert">&times;</button>
                @TempData["SuccessMessage"]
            </div>
        }
        @if (TempData["ErrorMessage"] != null)
        {
            <div class="alert alert-danger alert-dismissible fade show">
                <button type="button" class="close" data-dismiss="alert">&times;</button>
                @TempData["ErrorMessage"]
            </div>
        }

        <div class="row">
            <div class="col-xxl-2 col-xl-2 col-lg-2 col-md-2 col-sm-12 col-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Administration</h3>
                    </div>
                    <div class="card-body p-2">
                        <div class="nav flex-column nav-pills">
                            <a href="@Url.Action("Index", "PerformanceMetric")" class="nav-link @(currentController == "PerformanceMetric" ? "active" : "")">
                                Operational reports
                            </a>
                            <a href="@Url.Action("Index", "EnterpriseMetric")" class="nav-link @(currentController == "EnterpriseMetric" ? "active" : "")">
                                Enterprise metrics
                            </a>
                            <a href="@Url.Action("Index", "FunctionalStandard")" class="nav-link @(currentController == "FunctionalStandard" ? "active" : "")">
                                Functional standards
                            </a>
                            <a href="@Url.Action("Objectives", "Admin")" class="nav-link @(currentAction.StartsWith("Objective") ? "active" : "")">
                                Priority outcomes
                            </a>
                            <a href="@Url.Action("Missions", "Admin")" class="nav-link @(currentAction.StartsWith("Mission") ? "active" : "")">
                                Missions
                            </a>
                            <a href="@Url.Action("FundingSources", "Admin")" class="nav-link @(currentAction.Contains("FundingSource") ? "active" : "")">
                                Funding sources
                            </a>
                            <a href="@Url.Action("Index", "GovernmentDepartment")" class="nav-link @(currentController == "GovernmentDepartment" ? "active" : "")">
                                Government departments
                            </a>
                            <a href="@Url.Action("Index", "Organizational")" class="nav-link @(currentController == "Organizational" ? "active" : "")">
                                Organizational
                            </a>
                            <a href="@Url.Action("RiskTypes", "Admin")" class="nav-link @(currentAction.Contains("RiskType") ? "active" : "")">
                                Risk types
                            </a>
                            <a href="@Url.Action("RiskTiers", "Admin")" class="nav-link @(currentAction.Contains("RiskTier") ? "active" : "")">
                                Risk tiers
                            </a>
                            <a href="@Url.Action("ActionSources", "Admin")" class="nav-link @(currentAction.Contains("ActionSource") ? "active" : "")">
                                Action sources
                            </a>
                            <a href="@Url.Action("DeliveryPriorities", "Admin")" class="nav-link @(currentAction.Contains("DeliveryPriorit") ? "active" : "")">
                                Delivery priorities
                            </a>
                            <a href="@Url.Action("WcagCriteria", "Admin")" class="nav-link @(currentAction.Contains("WcagCriterion") || currentAction.Contains("WcagCriteria") ? "active" : "")">
                                WCAG Criteria
                            </a>
                            <a href="@Url.Action("BusinessAreas", "Admin")" class="nav-link @(currentAction.Contains("BusinessArea") ? "active" : "")">
                                Business areas
                            </a>
                            <a href="@Url.Action("Phases", "Admin")" class="nav-link @(currentAction.Contains("Phase") ? "active" : "")">
                                Phases
                            </a>
                            <a href="@Url.Action("RagStatuses", "Admin")" class="nav-link @(currentAction.Contains("RagStatus") ? "active" : "")">
                                RAG Statuses
                            </a>
                            <a href="@Url.Action("GddRoles", "Admin")" class="nav-link @(currentAction.Contains("GddRole") ? "active" : "")">
                                GDD Roles
                            </a>
                            <a href="@Url.Action("Skills", "Admin")" class="nav-link @(currentAction.Contains("Skill") ? "active" : "")">
                                Skills
                            </a>
                            <a href="@Url.Action("KpiCategories", "Admin")" class="nav-link @(currentAction.Contains("KpiCategor") ? "active" : "")">
                                KPI categories
                            </a>
                            <a href="@Url.Action("ApiTokens", "Admin")" class="nav-link @(currentAction.Contains("ApiToken") ? "active" : "")">
                                API tokens
                            </a>
                            <a href="@Url.Action("Index", "Admin", new { area = "" })" class="nav-link @(currentAction == "Settings" ? "active" : "")">
                                User management
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-xxl-10 col-xl-10 col-lg-10 col-md-10 col-sm-12 col-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">RAG Statuses</h3>
                    </div>
                    <div class="card-body">
                <p class="text-muted">Manage RAG status options for projects. Set the CSS class for each status to control badge styling.</p>

                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead>
                            <tr>
                                <th class="col-width-60px">Order</th>
                                <th>Name</th>
                                <th>Description</th>
                                <th>CSS Class</th>
                                <th>Status</th>
                                <th class="col-actions">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.Any())
                            {
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td class="text-center"><span class="badge badge-secondary">@item.SortOrder</span></td>
                                        <td><strong>@item.Name</strong></td>
                                        <td>
                                            @if (!string.IsNullOrEmpty(item.Description))
                                            {
                                                <small class="text-muted">@item.Description</small>
                                            }
                                            else
                                            {
                                                <span>-</span>
                                            }
                                        </td>
                                        <td>
                                            @if (!string.IsNullOrEmpty(item.CssClass))
                                            {
                                                <code>@item.CssClass</code>
                                            }
                                            else
                                            {
                                                <span class="text-muted">-</span>
                                            }
                                        </td>
                                        <td>
                                            @if (item.IsActive)
                                            {
                                                <span class="badge badge-success">Active</span>
                                            }
                                            else
                                            {
                                                <span class="badge badge-secondary">Inactive</span>
                                            }
                                        </td>
                                        <td>
                                            <div class="btn-group btn-group-sm" role="group">
                                                <button type="button" class="btn btn-outline-primary edit-rag-status-btn" 
                                                        data-rag-status-id="@item.Id"
                                                        data-rag-status-name="@Html.Raw(item.Name.Replace("'", "\\'"))"
                                                        data-rag-status-description="@Html.Raw(item.Description?.Replace("'", "\\'") ?? "")"
                                                        data-rag-status-sortorder="@item.SortOrder"
                                                        data-rag-status-isactive="@(item.IsActive ? "true" : "false")"
                                                        data-rag-status-cssclass="@Html.Raw(item.CssClass?.Replace("'", "\\'") ?? "")"
                                                        title="Edit">
                                                    Edit
                                                </button>
                                                <form asp-action="DeleteRagStatus" asp-route-id="@item.Id" method="post" class="d-inline delete-rag-status-form">
                                                    @Html.AntiForgeryToken()
                                                    <button type="submit" class="btn btn-outline-danger" title="Delete">
                                                        Delete
                                                    </button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="6" class="text-center text-muted">No RAG statuses found.</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Create RAG Status Modal -->
<div class="modal fade" id="createRagStatusModal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Create RAG Status</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <form asp-action="CreateRagStatus" method="post">
                @Html.AntiForgeryToken()
                <div class="modal-body">
                    <div class="form-group">
                        <label for="ragStatusName">Name</label>
                        <input type="text" class="form-control" id="ragStatusName" name="name" required maxlength="100">
                    </div>
                    <div class="form-group">
                        <label for="ragStatusDescription">Description</label>
                        <textarea class="form-control" id="ragStatusDescription" name="description" rows="3"></textarea>
                    </div>
                    <div class="form-group">
                        <label for="ragStatusSortOrder">Sort Order</label>
                        <input type="number" class="form-control" id="ragStatusSortOrder" name="sortOrder" value="0" min="0">
                        <small class="form-text text-muted">Lower numbers appear first</small>
                    </div>
                    <div class="form-group">
                        <label for="ragStatusCssClass">CSS Class</label>
                        <input type="text" class="form-control" id="ragStatusCssClass" name="cssClass" maxlength="100" placeholder="e.g., badge badge-green">
                        <small class="form-text text-muted">CSS class for styling the badge (e.g., badge badge-green)</small>
                    </div>
                    <div class="form-group">
                        <div class="form-check">
                            <input type="checkbox" class="form-check-input" id="ragStatusIsActive" name="isActive" value="true" checked>
                            <label class="form-check-label" for="ragStatusIsActive">Active</label>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-success">Create</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Edit RAG Status Modal -->
<div class="modal fade" id="editRagStatusModal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Edit RAG Status</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <form asp-action="EditRagStatus" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" id="editRagStatusId" name="id">
                <div class="modal-body">
                    <div class="form-group">
                        <label for="editRagStatusName">Name</label>
                        <input type="text" class="form-control" id="editRagStatusName" name="name" required maxlength="100">
                    </div>
                    <div class="form-group">
                        <label for="editRagStatusDescription">Description</label>
                        <textarea class="form-control" id="editRagStatusDescription" name="description" rows="3"></textarea>
                    </div>
                    <div class="form-group">
                        <label for="editRagStatusSortOrder">Sort Order</label>
                        <input type="number" class="form-control" id="editRagStatusSortOrder" name="sortOrder" min="0">
                    </div>
                    <div class="form-group">
                        <label for="editRagStatusCssClass">CSS Class</label>
                        <input type="text" class="form-control" id="editRagStatusCssClass" name="cssClass" maxlength="100" placeholder="e.g., badge badge-green">
                        <small class="form-text text-muted">CSS class for styling the badge (e.g., badge badge-green)</small>
                    </div>
                    <div class="form-group">
                        <div class="form-check">
                            <input type="checkbox" class="form-check-input" id="editRagStatusIsActive" name="isActive" value="true">
                            <label class="form-check-label" for="editRagStatusIsActive">Active</label>
                            <input type="hidden" name="isActive" value="false">
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-success">Save Changes</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
<script nonce="@Context.Items["Nonce"]">
(function() {
    function initRagStatusesPage() {
        // Handle edit RAG status button clicks
        $(document).on('click', '.edit-rag-status-btn', function() {
            const id = $(this).data('rag-status-id');
            const name = $(this).data('rag-status-name');
            const description = $(this).data('rag-status-description') || '';
            const sortOrder = $(this).data('rag-status-sortorder');
            const isActive = $(this).data('rag-status-isactive');
            const cssClass = $(this).data('rag-status-cssclass') || '';
            
            $('#editRagStatusId').val(id);
            $('#editRagStatusName').val(name);
            $('#editRagStatusDescription').val(description);
            $('#editRagStatusSortOrder').val(sortOrder);
            $('#editRagStatusCssClass').val(cssClass);
            
            // Handle checkbox - convert string "true"/"false" to boolean
            const isActiveBool = isActive === true || isActive === 'true' || String(isActive).toLowerCase() === 'true';
            $('#editRagStatusIsActive').prop('checked', isActiveBool);
            
            // Update hidden field based on checkbox state
            $('#editRagStatusModal input[type="hidden"][name="isActive"]').val(isActiveBool ? 'true' : 'false');
            
            // Listen for checkbox changes
            $('#editRagStatusIsActive').off('change').on('change', function() {
                $('#editRagStatusModal input[type="hidden"][name="isActive"]').val($(this).is(':checked') ? 'true' : 'false');
            });
            
            $('#editRagStatusModal').modal('show');
        });
        
        // Handle delete RAG status form submissions
        $(document).on('submit', '.delete-rag-status-form', function(e) {
            if (!confirm('Are you sure you want to delete this RAG status?')) {
                e.preventDefault();
                return false;
            }
            return true;
        });
    }

    // Wait for jQuery to be available
    if (typeof jQuery !== 'undefined') {
        $(document).ready(initRagStatusesPage);
    } else {
        document.addEventListener('DOMContentLoaded', function() {
            // Try again after a short delay to ensure jQuery is loaded
            var checkJQuery = setInterval(function() {
                if (typeof jQuery !== 'undefined') {
                    clearInterval(checkJQuery);
                    $(document).ready(initRagStatusesPage);
                }
            }, 50);
            
            // Stop trying after 5 seconds
            setTimeout(function() {
                clearInterval(checkJQuery);
            }, 5000);
        });
    }
})();
</script>
}
