@model Compass.ViewModels.Modern.ServiceLineListViewModel
@{
    ViewData["Title"] = "Service lines — Compass";
}

@section Breadcrumbs {
 <li class="govuk-breadcrumbs__list-item"><a class="govuk-breadcrumbs__link" href="@Url.Action("Index", "ModernDashboard")">Home</a></li>
 <li class="govuk-breadcrumbs__list-item"><a class="govuk-breadcrumbs__link" href="@Url.Action("Fips", "ModernManage")">Service Register</a></li>
 <li class="govuk-breadcrumbs__list-item" aria-current="page">Service lines</li>
}

@if (TempData["Success"] != null)
{
 <div class="govuk-notification-banner govuk-notification-banner--success" role="region" aria-labelledby="sl-success-title">
 <div class="govuk-notification-banner__header">
 <h2 class="govuk-notification-banner__title" id="sl-success-title">Success</h2>
        </div>
 <div class="govuk-notification-banner__content">
 <p class="govuk-notification-banner__heading">@TempData["Success"]</p>
        </div>
    </div>
}

<div class="dfe-c-flex--between dfe-c-mb-3" style="flex-wrap:wrap;gap:1rem;align-items:flex-end;">
 <div class="dfe-c-page-title" style="margin:0;">
 <div class="dfe-c-page-title__caption">Service Register</div>
 <h1 class="dfe-c-page-title__heading">Service lines</h1>
    </div>
 @if (Model.CanEdit)
 {
 <a class="govuk-button" href="@Url.Action("ServiceLineNew", "ModernManage")">Add service line</a>
    }
</div>

<p class="govuk-body govuk-!-margin-bottom-4">Group directorates, business areas, service register products, and work items for portfolio reporting.</p>

@if (Model.Rows.Count == 0)
{
 <p class="govuk-body">No service lines yet.</p>
}
else
{
 <div class="dfe-c-table-wrap">
 <table>
 <caption class="govuk-visually-hidden">Service lines</caption>
                <thead>
                    <tr>
 <th scope="col">Name</th>
 <th scope="col">Directorates</th>
 <th scope="col">Business areas</th>
 <th scope="col">Products</th>
 <th scope="col">Work items</th>
 <th scope="col">Updated</th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var r in Model.Rows)
                {
                    <tr>
                        <td>
 <a href="@Url.Action("ServiceLineDetail", "ModernManage", new { slug = r.Slug })" class="dfe-c-link dfe-c-fw-bold">@r.Name</a>
                        </td>
                        <td>@r.Directorates</td>
                        <td>@r.BusinessAreas</td>
                        <td>@r.Products</td>
                        <td>@r.WorkItems</td>
                        <td class="govuk-body-s">@r.UpdatedAt.ToString("d MMM yyyy")</td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
}
