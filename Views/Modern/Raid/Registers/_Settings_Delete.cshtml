@model Compass.ViewModels.Modern.RaidRegisterOnboardingViewModel

<p class="govuk-body">
  Delete this RAID register if it is no longer needed. This removes the register only —
  risks, issues, assumptions, dependencies and near misses linked to it are not deleted.
</p>
<p class="govuk-body govuk-!-margin-bottom-0">
  You will lose access to this register view. Other registers that track the same records are not affected.
</p>
