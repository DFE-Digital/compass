@model decimal?
@if (!Model.HasValue)
{
  <span class="dfe-c-text-muted">—</span>
}
else
{
  <span class="raid-ss-score-badge @RaidRegisterTableFormatting.RiskScoreBandClass(Model)">@($"{Model.Value:0}/25")</span>
}
