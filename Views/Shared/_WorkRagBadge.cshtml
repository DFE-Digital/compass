@model Compass.Models.ChromeRagSnapshot?
@using Compass.Models.Modern.Work
@{
    var rs = Model?.RagStatus;
    var name = rs?.Name;
    var tagClass = WorkBadgeCss.RagDfeTagClass(null, string.IsNullOrEmpty(name) ? "—" : name);
    var label = string.IsNullOrEmpty(name) ? "—" : name.ToUpperInvariant();
}
<span class="@tagClass">@label</span>
