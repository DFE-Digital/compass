@model Compass.Models.Project

@{
    var milestones = Model.Milestones
        .OrderBy(m => m.DueDate)
        .ToList();
    var completedMilestones = milestones.Count(m => string.Equals(m.Status, "complete", StringComparison.OrdinalIgnoreCase));
    var activeMilestones = milestones.Count - completedMilestones;
    var overdueMilestones = milestones.Count(m =>
        !string.Equals(m.Status, "complete", StringComparison.OrdinalIgnoreCase) &&
        m.DueDate.Date < DateTime.Today);
}

<div class="card" id="milestones">
    <div class="card-header">
        <h3 class="card-title">Project milestones</h3>
        <div class="card-tools">
            <a asp-controller="Project" asp-action="CreateMilestone" asp-route-projectId="@Model.Id">Add milestone</a>
        </div>
    </div>
    <div class="card-body table-responsive p-0">
        @if (milestones.Any())
        {
            <div class="row no-gutters text-center border-bottom">
                <div class="col-md-4 p-3">
                    <p class="text-uppercase text-muted mb-1 small">Completed</p>
                    <p class="h4 mb-0">@completedMilestones</p>
                </div>
                <div class="col-md-4 p-3 border-left border-right">
                    <p class="text-uppercase text-muted mb-1 small">Active</p>
                    <p class="h4 mb-0">@activeMilestones</p>
                </div>
                <div class="col-md-4 p-3">
                    <p class="text-uppercase text-muted mb-1 small">Overdue</p>
                    <p class="h4 mb-0 text-danger">@overdueMilestones</p>
                </div>
            </div>

            <table class="table table-hover text-nowrap sortable-table milestones-table">
                <thead>
                    <tr>
                        <th class="col-name sortable" data-column="Name" tabindex="0" role="button" aria-label="Sort by Name">Name</th>
                        <th class="col-status sortable" data-column="Status" tabindex="0" role="button" aria-label="Sort by Status">Status</th>
                        <th class="col-due sortable" data-column="DueDate" tabindex="0" role="button" aria-label="Sort by Due Date">Due date</th>
                        <th class="col-actual sortable" data-column="ActualDate" tabindex="0" role="button" aria-label="Sort by Actual Date">Actual date</th>
                        <th class="col-progress sortable" data-column="Progress" tabindex="0" role="button" aria-label="Sort by Progress">Progress</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var milestone in milestones)
                    {
                        <tr>
                            <td>
                                <a asp-controller="Project" asp-action="MilestoneDetails" asp-route-id="@milestone.Id">
                                    @milestone.Name
                                </a>
                            </td>
                            <td>
                                @{
                                    var statusClass = milestone.Status switch
                                    {
                                        "complete" => "badge badge-success",
                                        "at_risk" => "badge badge-warning",
                                        "in_progress" => "badge badge-info",
                                        "delayed" => "badge badge-danger",
                                        _ => "badge badge-secondary"
                                    };
                                }
                                <span class="@statusClass">@milestone.Status.Replace("_", " ")</span>
                            </td>
                            <td>@milestone.DueDate.ToString("d MMMM yyyy")</td>
                            <td>
                                @if (milestone.ActualDate.HasValue)
                                {
                                    @milestone.ActualDate.Value.ToString("d MMMM yyyy")
                                }
                                else
                                {
                                    <span class="text-muted">Not completed</span>
                                }
                            </td>
                            <td style="min-width: 150px;">
                                @{
                                    var progress = milestone.ProgressPercent.GetValueOrDefault();
                                    var progressValue = progress < 0 ? 0 : progress > 100 ? 100 : progress;
                                }
                                <div class="d-flex align-items-center">
                                    <div class="progress flex-grow-1 mr-2" style="height: 6px;">
                                        <div class="progress-bar bg-success" role="progressbar" style="width: @progressValue%" aria-valuenow="@progressValue" aria-valuemin="0" aria-valuemax="100"></div>
                                    </div>
                                    <span class="text-muted small">@progressValue%</span>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="p-4">
                <p class="text-muted mb-2">No milestones recorded.</p>
                <a asp-controller="Project" asp-action="CreateMilestone" asp-route-projectId="@Model.Id">Add the first milestone</a>
            </div>
        }
    </div>
</div>
